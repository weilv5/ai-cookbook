name = "openai-gemini-proxy"
main = "src/index.js"
compatibility_date = "2024-04-01"
compatibility_flags = ["nodejs_compat"]

# 如果使用自定义域名，取消下面这行注释并修改为你的域名
# route = "api.yourdomain.com/*"

# 无需自定义域名时，会自动分配 *.workers.dev 域名
